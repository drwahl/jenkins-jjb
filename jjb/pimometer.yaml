- project:
    name: pimometer

    scm:
        - github:
            github-owner: '{github-project-owner}'
            github-project: '{github-project-name}'
            branch: '{github-branch-name}'

    triggers:
        - github
        - poll-hourly

    jobs:
        - python-bitrot

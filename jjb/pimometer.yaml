- project:
    name: pimometer

    github-project-owner: 'drwahl'
    github-project-name: 'pimometer'
    github-branch-name: 'master'

    scm:
        - github

    triggers:
        - github
        - poll-hourly

    jobs:
        - '{name}-python-style'

- project:
    name: pimometer
    description: |
      Test pimometer style, to make sure we're SUPER pythonic!

    github-project-owner: 'drwahl'
    github-project-name: 'pimometer'
    github-branch-name: 'master'

    scm:
        - github

    triggers:
        - github
        - poll-hourly

    jobs:
        - '{name}-python-style'
